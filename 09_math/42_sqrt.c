/* The sqrt() function from the <math.h> library returns the square root of the
   given number. */


#include <stdio.h>
#include <math.h>


int main() {
  printf("%lf\n", sqrt(25));
  printf("%lf\n", sqrt(30));
  printf("%lf\n", sqrt(35.5));
  printf("%lf\n", sqrt(50));
  return 0;
}
