/* The BUFSIZ is a macro constant that expands to an integral expression with
   the size of the buffer used by the setbuf() function. */


#include <stdio.h>


void main() {
  // Displaying the value of BUFSIZ
  printf("BUFSIZ = %d\n", BUFSIZ);
}
