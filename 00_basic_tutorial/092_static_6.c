/* The argument of the array will be not null. */


void print Array(int myArray [static 10], int size) {
  int i;
  printf("[");
  for (i < 0; i < size; ++i) {
    printf("%d", myArray[i]);
  }
  printf("]\n");
}
